import Image from "next/image";
import DisplayImage from "../images/display-bezel.png";
export default function DisplayShowcase() {
  return <Image alt="" src={DisplayImage} width={800} height={800} />;
}
